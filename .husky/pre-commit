if command -v gitleaks >/dev/null 2>&1; then
  gitleaks protect --staged --no-banner
else
  echo "❌ ERROR: gitleaks not installed — secret scanning is required."
  echo "   Install: https://github.com/gitleaks/gitleaks#installing"
  exit 1
fi
npx lint-staged
