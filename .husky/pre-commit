if command -v gitleaks >/dev/null 2>&1; then
  gitleaks protect --staged --no-banner
else
  echo "⚠️  WARNING: gitleaks not installed — secret scanning skipped. Install: https://github.com/gitleaks/gitleaks#installing"
fi
npx lint-staged
